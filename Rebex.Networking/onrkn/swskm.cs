namespace onrkn;

internal class swskm : mkuxt
{
	private readonly int mdmqh;

	private readonly int hbtxc;

	private readonly int vitgh;

	public override void jfjrs(tndeg p0)
	{
		mkuxt.agnqw(p0, 34);
		mkuxt.ebmel(p0, (uint)mdmqh);
		mkuxt.ebmel(p0, (uint)hbtxc);
		mkuxt.ebmel(p0, (uint)vitgh);
	}

	public swskm(int min, int size, int max)
	{
		mdmqh = min;
		hbtxc = size;
		vitgh = max;
	}
}
