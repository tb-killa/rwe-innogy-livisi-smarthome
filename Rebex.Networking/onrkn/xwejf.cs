namespace onrkn;

internal class xwejf : mkuxt
{
	private uint dbzie;

	private byte[] ayegn;

	private int bnkpu;

	private int cztza;

	public override void jfjrs(tndeg p0)
	{
		mkuxt.agnqw(p0, 94);
		mkuxt.ebmel(p0, dbzie);
		mkuxt.girvs(p0, ayegn, bnkpu, cztza, p4: false);
	}

	public xwejf(uint recipientChannel, byte[] buffer, int offset, int count)
	{
		dbzie = recipientChannel;
		ayegn = buffer;
		bnkpu = offset;
		cztza = count;
	}
}
