using Rebex.Net;

namespace onrkn;

internal class sicek : webdt
{
	private static readonly webdt bhbwm = new sicek();

	public static webdt edema => bhbwm;

	private sicek()
	{
	}

	public TlsSession bfjil(byte[] p0)
	{
		TlsSession.atnfa();
		string p1 = TlsSession.pwkvr(p0);
		return TlsSession.relpg(p1);
	}

	public void lacjz(byte[] p0, TlsSession p1)
	{
		string p2 = TlsSession.pwkvr(p0);
		TlsSession.mcihc(p2, p1);
	}
}
