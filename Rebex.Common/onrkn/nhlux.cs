using System;
using Rebex.Security.Authentication;

namespace onrkn;

internal class nhlux : cixll
{
	protected override void tstpp(bool p0)
	{
		if (inyna() != IntPtr.Zero && 0 == 0 && (!base.zuofh || 1 == 0))
		{
			SspiAuthentication.DeleteSecurityContext(inyna());
			zhtek();
		}
		base.tstpp(p0);
	}
}
